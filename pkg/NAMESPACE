## package has a dynamic library
useDynLib(hawkes)

## and some exported functions
exportPattern("^[[:alpha:]]+")
#exportPattern("^[[:alnum:]%\\[0-9$<-]+")
## And imports
import( Rcpp )
import( RcppArmadillo )
